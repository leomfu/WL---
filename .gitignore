# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# 视觉稿画布的中间产物（画板源文件 *.dc.html 和 canvas.json 要提交）
docs/design/__pycache__/

# 摄影原图：只在本机放着，`npm run photos` 压出来的 webp 才进仓库
# （原图动辄几 MB 一张，进了 git 就再也拿不出来了）
/photos-src/

# 首页 hero 的原图：同上，只在本机放着，`npm run hero` 压出来的 webp 才进仓库
/hero-src/
